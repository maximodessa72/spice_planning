# ИТЕРАТИВНЫЙ ПОИСК T — Одинаковый буфер для всех позиций

## КОНЦЕПЦИЯ

**Цель:** Все позиции должны получить **одинаковый буфер** (закончиться одновременно)

**Проблема с текущей логикой:**
```python
T = (контейнер + остатки) / планы  # Расчёт без учёта округления
opts[позиция] = round_to(T × план - остаток, 25)  # Округление каждой позиции

Проблема: сумма округлённых заказов ≠ контейнер
→ Нужна подгонка → урезание → неравномерные буферы
```

## РЕШЕНИЕ: Бинарный поиск оптимального T

### Алгоритм:

```
1. Начальная оценка: T = (контейнер + остатки) / планы
2. Границы поиска: T_min = 0, T_max = T × 2

Цикл (макс 50 итераций):
    3. Рассчитываем заказы с текущим T (с округлением)
    4. Считаем сумму заказов
    5. Если сумма > контейнер → T слишком большой → T_max = T
    6. Если сумма < контейнер → T слишком маленький → T_min = T
    7. Новый T = (T_min + T_max) / 2
    8. Если |сумма - контейнер| ≤ 25 кг (1 мешок) → СТОП

9. Используем найденный T для финального расчёта
```

### Пример работы:

**Исходные данные:**
- Контейнер: 40000 кг
- Остатки: 14084 кг
- Планы: 34614 кг

**Итерация 1:**
```
T = 1.5625
Заказы (округлённые): 8300 + 7650 + 8175 + ... = 40500 кг
Сумма > контейнер → уменьшаем T
T_max = 1.5625
T_new = (0 + 1.5625) / 2 = 0.78125
```

**Итерация 2:**
```
T = 0.78125
Заказы: 4150 + 3825 + 4100 + ... = 27000 кг
Сумма < контейнер → увеличиваем T
T_min = 0.78125
T_new = (0.78125 + 1.5625) / 2 = 1.171875
```

**...**

**Итерация 15:**
```
T = 1.5234
Заказы: 8075 + 7475 + 7975 + ... = 39975 кг
|39975 - 40000| = 25 кг ≤ допуск → СТОП!
```

**Результат:**
```
Все позиции получают заказ с коэффициентом T = 1.5234
→ Буфер всех позиций ≈ 1.52 месяца
→ Все закончатся одновременно ✅
```

## ПРЕИМУЩЕСТВА

1. **Одинаковый буфер** для всех позиций (главная цель!)
2. **Все закончатся одновременно** → оптимальное планирование следующего заказа
3. **Контейнер заполнен максимально** (±25 кг = 1 мешок)
4. **Не нужна грубая подгонка** с урезанием позиций

## ФИНАЛЬНАЯ ПОДГОНКА

Если после поиска T разница всё равно > 25 кг (редко):
- **Излишек:** урезаем из позиций с большим остатком (как раньше)
- **Недостаток:** добавляем в позицию с наибольшим планом

Но обычно бинарный поиск даёт результат ±25 кг за 10-20 итераций.

## РЕЗУЛЬТАТ ДЛЯ ПЕТРУШКИ (В)

**Август:**
```
T (итеративный) ≈ 1.52 (вместо 1.5625)
Заказ петрушки = 1.52 × 5300 - 0 = 8056 кг (вместо 1175!)
Буфер после = 8056 / 5300 ≈ 1.52 месяца

ВСЕ позиции получат буфер ≈ 1.52:
- Базилик (В): буфер 1.52 ✅
- Укроп (В): буфер 1.52 ✅
- Петрушка (В): буфер 1.52 ✅
- Лемонграс: буфер 1.52 ✅
```

Все закончатся одновременно через 1.52 месяца!

## ПАРАМЕТРЫ

- **Максимум итераций:** 50 (обычно хватает 15-20)
- **Допуск:** ±25 кг (1 мешок)
- **Округление:** вниз допускается (контейнер ≤ 40000)

## ФАЙЛ

`simulation.py` — функция `opt_std` с итеративным поиском T
