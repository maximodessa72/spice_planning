# ИСПРАВЛЕНИЕ: Единая формула балансировки для всех позиций

## ПРОБЛЕМА

**В'єтнам перець, ноябрь:**
```
Білий: заказ=1400 → буфер после 2.28
FAQ:   заказ=1825 → буфер после 1.27 ❌
ASTA:  заказ=24775 → буфер после 2.66
```

Буферы **РАЗНЫЕ!** Должны быть одинаковые.

## ПРИЧИНА

В opt_std было **ДВЕ** разных формулы:

### БЫЛО (неправильно):

```python
if balance_start >= plan_full:
    # Товара хватало
    raw_order = T × plan - balance  ← НЕПРАВИЛЬНО!
else:
    # Товара не хватало
    raw_order = T × plan + plan×0.67
```

**Для FAQ:**
- Остаток: 9143 кг
- План: 4826 кг
- 9143 >= 4826 → TRUE → использовалась формула "хватало"
- Заказ = T×4826 - 9143 = 1825
- **Буфер после = 1.27** (совсем не T!)

## РЕШЕНИЕ

**ЕДИНАЯ формула** для ВСЕХ позиций (независимо от остатка):

```python
# Для ВСЕХ позиций:
заказ = T × план + план×0.67 - остаток
```

### Логика:

```
Целевой буфер после прихода = T
Остаток после = остаток + заказ - план×0.67

T × план = остаток + заказ - план×0.67
заказ = T × план + план×0.67 - остаток
```

### Проверка для FAQ:

```
T = 2.2713
План = 4826
Остаток = 9143

Заказ = 2.2713×4826 + 4826×0.67 - 9143
      = 10960 + 3234 - 9143
      = 5051 кг

Буфер после = (9143 + 5051 - 4826×0.67) / 4826
            = (14194 - 3234) / 4826
            = 10960 / 4826
            = 2.27 ≈ T ✅
```

Теперь **ВСЕ** позиции получат буфер ≈ T!

## КОД

### Итеративный поиск T:
```python
for iteration in range(50):
    for it in items:
        balance_start = ibs[it["name"]]
        plan_full = it["plan"]
        
        # ЕДИНАЯ ФОРМУЛА для всех
        raw_order = T * plan_full + plan_full * 0.67 - balance_start
        opts_temp[it["name"]] = round_to(max(0, raw_order), 25)
    
    # Корректируем T если сумма ≠ контейнер
```

### Финальный расчёт:
```python
# С найденным оптимальным T
for it in items:
    raw_order = best_T * plan + plan * 0.67 - balance
    opts[it["name"]] = round_to(max(0, raw_order), 25)
```

## РЕЗУЛЬТАТ

**Теперь ВСЕ позиции** в контейнере получат **одинаковый буфер** = T!

```
Білий: буфер ≈ 2.27
FAQ:   буфер ≈ 2.27 ✅
ASTA:  буфер ≈ 2.27 ✅
```

**Все закончатся одновременно!** 🎯

## ТЕСТИРОВАНИЕ

1. Замени simulation.py
2. Запусти приложение
3. Смотри DEBUG вывод для Вьетнам перець
4. Проверь что буферы ОДИНАКОВЫЕ для всех позиций

## ФАЙЛ

`simulation.py` — единая формула балансировки
