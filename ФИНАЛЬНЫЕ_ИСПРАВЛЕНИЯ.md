# ФИНАЛЬНЫЕ ИСПРАВЛЕНИЯ: Полная синхронизация week_arrival

## ИСПРАВЛЕНО: 4 файла

### Проблемы были в ДВУХ местах:

1. **simulation.py** — НЕ учитывал `week_arrival` при расчёте `wl`
2. **excel_export.py** — НЕ использовал `wl` из результатов, пересчитывал из буфера

---

## 1. simulation.py

**Строка ~278-279:**

```python
# БЫЛО:
w_buf_before = round(wn / wd, 2) if wd > 0 else 99
wl, wc = get_week(w_buf_before)

# СТАЛО:
w_buf_before = round(wn / wd, 2) if wd > 0 else 99

# Проверяем есть ли зафиксированный номер недели в week_arrival
if "week_arrival" in group and i in group["week_arrival"]:
    week_num = group["week_arrival"][i]
    # Формируем метку из номера недели
    week_labels = {
        1: ("Тиж. 1", "E65100"),
        2: ("Тиж. 2", "F9A825"),
        3: ("Тиж. 3", "2E7D32"),
        4: ("Тиж. 4", "1565C0")
    }
    wl, wc = week_labels.get(week_num, get_week(w_buf_before))
else:
    # Автоматическое определение по буферу
    wl, wc = get_week(w_buf_before)
```

---

## 2. excel_export.py

**Строка ~264-277:**

```python
# БЫЛО:
# 3. Приход
c = ws.cell(row=data_row, column=col + 2)
if r["arrive"] > 0:
    week_label = get_week_label(r["w_buf_before"])  # ❌ Пересчёт из буфера
    
    # ФИКСИРОВАННЫЕ заказы (in_transit) - ВСЕГДА коричневые
    if r["in_transit"]:

# СТАЛО:
# 3. Приход
c = ws.cell(row=data_row, column=col + 2)
if r["arrive"] > 0:
    # ИСПОЛЬЗУЕМ week_label из симуляции (учитывает week_arrival)
    week_label_full = r.get("wl", "")
    
    # Конвертируем метку из simulation.py в краткий формат
    if "пред" in week_label_full.lower():
        week_label = "пред.мес"
    elif "Тиж. 1" in week_label_full:
        week_label = "Нед.1"
    elif "Тиж. 2" in week_label_full:
        week_label = "Нед.2"
    elif "Тиж. 3" in week_label_full:
        week_label = "Нед.3"
    elif "Тиж. 4" in week_label_full:
        week_label = "Нед.4"
    else:
        # Fallback на старый метод если метка не распознана
        week_label = get_week_label(r["w_buf_before"])
    
    # ФИКСИРОВАННЫЕ заказы (in_transit) - ВСЕГДА коричневые
    if r["in_transit"]:
```

**Строка ~291-293:**

```python
# БЫЛО:
else:
    # НОВЫЕ заказы - цвет по метке недели
    week_color = get_week_color(r["w_buf_before"])  # ❌ Пересчёт из буфера

# СТАЛО:
else:
    # НОВЫЕ заказы - цвет из симуляции (учитывает week_arrival)
    week_color = r.get("wc", "D5F5E3")  # Используем wc из симуляции
```

---

## 3. app.py

**Изменение 1: Строка ~2962**
```python
orders_current_month.append({
    ...
    "_arrival_mi": procurement_current_mi  # ДОБАВЛЕНО
})
```

**Изменение 2: Строки ~3056-3081**
Фиксация даты с автообновлением `week_arrival` + пересчёт + DEBUG

---

## 4. sales_export.py

**6 изменений:**
- Добавлен параметр `group` в `calculate_working_days_for_month`
- Проверка `week_arrival` с приоритетом над `week_label`
- 3 обновлённых вызова функции

---

## КАК ЭТО РАБОТАЕТ ТЕПЕРЬ:

### Планирование ЗАКУПОК:
```
1. Редактирование недели заказа (например, с 2 на 3)
   ↓
2. week_arrival[mi] = 3 сохраняется
   ↓
3. run_all_simulations() пересчитывает
   ↓
4. simulation.py видит week_arrival[i]=3
   → wl = "Тиж. 3", wc = "2E7D32"
   ↓
5. excel_export.py использует r["wl"] и r["wc"]
   → Excel: "Нед.3" с зелёным фоном ✅
```

### Планирование ПРОДАЖ:
```
1. Фиксация даты (15-е число)
   ↓
2. week_number = (15-1)//7 + 1 = 3
   ↓
3. week_arrival[mi] = 3 сохраняется
   arrival_fixed_dates сохраняется
   ↓
4. run_all_simulations() пересчитывает
   ↓
5. simulation.py: wl = "Тиж. 3"
   ↓
6. sales_export.py:
   - Видит arrival_fixed_dates → точные рабочие дни
   - Видит week_arrival → использует для расчёта
   ↓
7. Excel: дата 15.05, неделя 3, остатки пересчитаны ✅
```

---

## ФАЙЛЫ В АРХИВЕ:

1. **simulation.py** — проверка `week_arrival` перед расчётом `wl`
2. **excel_export.py** — использование `r["wl"]` и `r["wc"]` вместо пересчёта
3. **app.py** — фиксация даты + DEBUG
4. **sales_export.py** — использование `week_arrival` для рабочих дней

---

## ТЕСТИРОВАНИЕ:

### 1. Планирование закупок:
- Подтверждённые заказы → Редактировать
- Изменить неделю с 2 на 3 → Сохранить
- Экспорт закупок → Проверить "Нед.3" в Excel ✅

### 2. Планирование продаж:
- Фиксация прихода
- Изменить дату на 15-е → Зафиксировать
- Проверить DEBUG: `arrival_mi=0, week_number=3`
- Экспорт продаж → Проверить дату, неделю, остатки ✅

---

## ВСЁ ГОТОВО!

Все 4 файла исправлены и синхронизированы.
Изменения week_arrival теперь отражаются везде:
- ✅ В списках приложения
- ✅ В Excel закупок
- ✅ В Excel продаж
- ✅ В пересчёте остатков
