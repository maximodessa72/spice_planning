# ИСПРАВЛЕНИЯ: Синхронизация недель и дат прихода

## ПРОБЛЕМЫ:
1. При редактировании недели заказа → изменение не отражается в Excel и не идёт пересчёт остатков
2. При фиксации даты прихода → номер недели не меняется, в Excel неделя не меняется и не пересчитывается

---

## ИСПРАВЛЕНИЕ 1: app.py (строка ~2962)

**Найти:**
```python
                orders_current_month.append({
                    "group_name": group_name,
                    "containers": containers,
                    "arrival_kg": arrival_kg,
                    "week_label": week_label,
                    "auto_day": auto_day,
                    "current_date": current_date,
                    "is_fixed": is_fixed
                })
```

**Заменить на:**
```python
                orders_current_month.append({
                    "group_name": group_name,
                    "containers": containers,
                    "arrival_kg": arrival_kg,
                    "week_label": week_label,
                    "auto_day": auto_day,
                    "current_date": current_date,
                    "is_fixed": is_fixed,
                    "_arrival_mi": procurement_current_mi  # Месяц прихода для week_arrival
                })
```

---

## ИСПРАВЛЕНИЕ 2: app.py (строки ~3052-3059)

**Найти:**
```python
    with col3:
        st.write("")  # Отступ
        st.write("")
        if st.button("💾 Зафиксировать", type="primary", use_container_width=True):
            # Сохраняем дату
            st.session_state.arrival_fixed_dates[selected_order["group_name"]] = datetime.combine(new_date, datetime.min.time())
            st.success(f"✅ Дата для '{selected_order['group_name']}' зафиксирована: {new_date.strftime('%d.%m.%Y')}")
            st.rerun()
```

**Заменить на:**
```python
    with col3:
        st.write("")  # Отступ
        st.write("")
        if st.button("💾 Зафиксировать", type="primary", use_container_width=True):
            # Сохраняем дату
            fixed_datetime = datetime.combine(new_date, datetime.min.time())
            st.session_state.arrival_fixed_dates[selected_order["group_name"]] = fixed_datetime
            
            # Автоматически вычисляем номер недели из даты
            week_number = (new_date.day - 1) // 7 + 1  # Неделя 1-4
            
            # Находим группу и обновляем week_arrival
            for group in st.session_state.groups:
                if group["name"] == selected_order["group_name"]:
                    # Находим mi для текущего месяца
                    # selected_order содержит данные для текущего месяца
                    arrival_mi = selected_order.get("_arrival_mi", 0)  # mi месяца прихода
                    
                    if "week_arrival" not in group:
                        group["week_arrival"] = {}
                    group["week_arrival"][arrival_mi] = week_number
                    break
            
            # КРИТИЧНО: Пересчитываем симуляцию после изменения
            st.session_state.results = run_all_simulations(st.session_state.groups)
            st.session_state.need_recalc = False
            
            st.success(f"✅ Дата для '{selected_order['group_name']}' зафиксирована: {new_date.strftime('%d.%m.%Y')} (неделя {week_number})")
            st.rerun()
```

---

## ИСПРАВЛЕНИЕ 3: sales_export.py (строка ~145)

**Найти:**
```python
def calculate_working_days_for_month(mi: int, group_name: str, week_label: str, 
                                     arrival_kg: float, arrival_fixed_dates: Dict) -> int:
```

**Заменить на:**
```python
def calculate_working_days_for_month(mi: int, group_name: str, week_label: str, 
                                     arrival_kg: float, arrival_fixed_dates: Dict,
                                     group: Dict = None) -> int:
```

---

## ИСПРАВЛЕНИЕ 4: sales_export.py (строка ~146)

**В описании функции найти:**
```python
    """
    Рассчитать рабочие дни для месяца с учётом зафиксированных дат
    
    Args:
        mi: индекс месяца (для продаж: 0=прошлый, 1=текущий)
        group_name: название группы
        week_label: метка недели из симуляции
        arrival_kg: количество прихода
        arrival_fixed_dates: зафиксированные даты {group_name: datetime}
    
    Returns:
        Количество рабочих дней
    """
```

**Заменить на:**
```python
    """
    Рассчитать рабочие дни для месяца с учётом зафиксированных дат
    
    Args:
        mi: индекс месяца (для продаж: 0=прошлый, 1=текущий)
        group_name: название группы
        week_label: метка недели из симуляции
        arrival_kg: количество прихода
        arrival_fixed_dates: зафиксированные даты {group_name: datetime}
        group: данные группы (для week_arrival)
    
    Returns:
        Количество рабочих дней
    """
```

---

## ИСПРАВЛЕНИЕ 5: sales_export.py (строки ~183-193)

**Найти блок:**
```python
    else:
        # Для будущих месяцев используем недели
        if "Тиж. 1" in week_label or "пред" in week_label.lower():
            return WORKING_DAYS_BY_WEEK["нед 1"]
        elif "Тиж. 2" in week_label:
            return WORKING_DAYS_BY_WEEK["нед 2"]
        elif "Тиж. 3" in week_label:
            return WORKING_DAYS_BY_WEEK["нед 3"]
        elif "Тиж. 4" in week_label:
            return WORKING_DAYS_BY_WEEK["нед 4"]
        else:
            return WORKING_DAYS_BY_WEEK[None]
```

**Заменить на:**
```python
    else:
        # ПРИОРИТЕТ 1: Проверяем зафиксированный номер недели в week_arrival
        if group and "week_arrival" in group:
            week_num = group["week_arrival"].get(mi)
            if week_num:
                # Используем номер недели из week_arrival
                week_key = f"нед {week_num}"
                return WORKING_DAYS_BY_WEEK.get(week_key, WORKING_DAYS_BY_WEEK[None])
        
        # ПРИОРИТЕТ 2: Для будущих месяцев используем метку из симуляции
        if "Тиж. 1" in week_label or "пред" in week_label.lower():
            return WORKING_DAYS_BY_WEEK["нед 1"]
        elif "Тиж. 2" in week_label:
            return WORKING_DAYS_BY_WEEK["нед 2"]
        elif "Тиж. 3" in week_label:
            return WORKING_DAYS_BY_WEEK["нед 3"]
        elif "Тиж. 4" in week_label:
            return WORKING_DAYS_BY_WEEK["нед 4"]
        else:
            return WORKING_DAYS_BY_WEEK[None]
```

---

## ИСПРАВЛЕНИЕ 6: sales_export.py (строка ~428)

**Найти:**
```python
                # Рабочие дни (с учётом зафиксированных дат)
                working_days = calculate_working_days_for_month(
                    mi, group_name, week_label, arrival_kg, arrival_fixed_dates
                )
```

**Заменить на:**
```python
                # Рабочие дни (с учётом зафиксированных дат)
                working_days = calculate_working_days_for_month(
                    mi, group_name, week_label, arrival_kg, arrival_fixed_dates, group
                )
```

---

## ИСПРАВЛЕНИЕ 7: sales_export.py (строка ~680)

**Найти:**
```python
                # Рассчитываем рабочие дни с учётом зафиксированных дат
                working_days = calculate_working_days_for_month(
                    mi, group_name, week_label, arrival_kg, arrival_fixed_dates
                )
```

**Заменить на:**
```python
                # Рассчитываем рабочие дни с учётом зафиксированных дат
                working_days = calculate_working_days_for_month(
                    mi, group_name, week_label, arrival_kg, arrival_fixed_dates, group
                )
```

---

## ИСПРАВЛЕНИЕ 8: sales_export.py (строка ~840)

**Найти:**
```python
                # Рабочие дни (с учётом зафиксированных дат)
                working_days = calculate_working_days_for_month(
                    mi, group_name, week_label, arrival_kg, arrival_fixed_dates
                )
```

**Заменить на:**
```python
                # Рабочие дни (с учётом зафиксированных дат)
                working_days = calculate_working_days_for_month(
                    mi, group_name, week_label, arrival_kg, arrival_fixed_dates, group
                )
```

---

## ИТОГО:

- **2 изменения** в `app.py`
- **6 изменений** в `sales_export.py`

После применения всех исправлений:
✅ Редактирование недели заказа → сразу отражается в Excel
✅ Фиксация даты прихода → автоматически обновляет неделю и пересчитывает остатки
