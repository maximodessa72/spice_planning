# ВОССТАНОВЛЕННАЯ РАБОЧАЯ ВЕРСИЯ

## ЧТО ВОССТАНОВЛЕНО

Вернулись к рабочей версии которая содержит:

### ✅ 1. Пересчёт остатков с учётом недели прихода
- Функции: `get_arrival_day_from_week`, `calculate_proportional_plan`
- Логика: если товара не хватало и есть приход → пропорциональный план

### ✅ 2. Балансировка с учётом недели прихода (opt_std)
- Фиксированная неделя 2 (10-е число, коэффициент 0.67)
- Итеративный поиск T для одинакового буфера всех позиций
- Формула: `заказ = T × план + план × 0.67` (если товара не хватало)

### ✅ 3. СТАРАЯ логика проверки заказа (рабочая)
```python
bf = future_bal(balance + arrive, pending, i, cycle_m, group, buffer)
if bf < total_plan * buffer:
    pending[i + cycle_m] = ckg
```

## ЧТО УБРАНО

❌ Испорченная логика "буфер < 1.1 → приход в этом месяце" которая ломала симуляцию

## ИЗВЕСТНЫЕ ПРОБЛЕМЫ (которые можно исправить позже)

1. **Перезапас** — future_bal может заказывать слишком часто (как в Китай овощи)
2. **Не учитывает неделю** — future_bal использует полный план вместо пропорционального

НО система РАБОТАЕТ — все заказы на месте!

## ЧТО РАБОТАЕТ

✅ Фиксированные заказы отображаются  
✅ Нефиксированные заказы создаются  
✅ Балансировка даёт одинаковые буферы  
✅ Пересчёт остатков учитывает неделю  

## СЛЕДУЮЩИЕ ШАГИ

Когда захочешь исправить проблему перезапаса — можно:
1. Модифицировать `future_bal` чтобы учитывала пропорциональный план
2. Или добавить проверку "есть ли уже ожидаемый приход"

Но сейчас главное — **система работает**! 

## ФАЙЛ

`simulation.py` — восстановленная рабочая версия
