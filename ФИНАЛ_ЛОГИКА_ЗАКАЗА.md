# ФИНАЛЬНАЯ ЛОГИКА — Буфер < 1.1 → Приход в этом месяце

## ПРАВИЛЬНАЯ ЛОГИКА

```
Для каждого месяца i:
  1. Проверяем буфер на НАЧАЛО месяца i
  2. Если буфер < 1.1:
      → Нужен приход В ЭТОМ месяце i
      → Отсчитываем цикл назад: order_month = i - cycle_m
      → Размещаем: pending[i] = контейнер
      → Запоминаем: order_month (когда заказ был размещён)
```

## ПРИМЕР (Китай овощи, цикл 4 мес)

### Сентябрь (mi=5):
```
Буфер на начало сентября: 0.0 < 1.1
→ Нужен приход В СЕНТЯБРЕ
→ Заказ размещён: 5 - 4 = 1 (МАЙ)
→ pending[5] = 25000
→ order_month = 1
```

### Октябрь (mi=6):
```
Буфер на начало октября: 1.3 > 1.1
→ Приход НЕ нужен
→ Заказ НЕ размещается
```

### Ноябрь (mi=7):
```
Буфер на начало ноября: 0.3 < 1.1
→ Нужен приход В НОЯБРЕ
→ Заказ размещён: 7 - 4 = 3 (ИЮЛЬ)
→ pending[7] = 25000
→ order_month = 3
```

## РЕЗУЛЬТАТ

| Месяц (i) | Буфер начало | Приход нужен? | Заказ в месяце | Приход в месяце |
|-----------|--------------|---------------|----------------|-----------------|
| Май (1) | 1.0 | ❌ | — | — |
| Июнь (2) | 0.4 | ❌ | — | — |
| Июль (3) | 0.1 | ❌ | — | — |
| Август (4) | 1.7 | ❌ | — | — |
| Сентябрь (5) | 0.0 | ✅ | Май (1) | 25000 |
| Октябрь (6) | 1.3 | ❌ | — | — |
| Ноябрь (7) | 0.3 | ✅ | Июль (3) | 25000 |
| Декабрь (8) | 1.0 | ❌ | — | — |

**2 заказа** (в мае и июле) → 2 прихода (сентябрь, ноябрь) → **оптимально!** ✅

## КОД

```python
# Проверка необходимости заказа
order_threshold = 1.1

if w_buf_before < order_threshold:
    # Нужен приход в текущем месяце i
    order_month = i - cycle_m
    
    if order_month >= 0:
        # Заказ размещается в месяце order_month
        pending[i] = ckg
    else:
        # Если (i - cycle_m) < 0, заказ был нужен ДО начала горизонта
        # Размещаем экстренный приход
        pending[i] = ckg
        order_month = 0
```

## ОТЛИЧИЯ ОТ ПРЕДЫДУЩИХ ВЕРСИЙ

### ❌ БЫЛО (версия 1):
```python
if буфер_текущий < 1.1:
    pending[i + cycle_m] = ckg  # Приход через cycle_m
```
**Проблема:** Перезапас — каждый месяц заказывали новый контейнер

### ❌ БЫЛО (версия 2):
```python
if буфер_будущий < 1.1:  # Проверяем (i + cycle_m)
    pending[i + cycle_m] = ckg
```
**Проблема:** Сложный расчёт будущего баланса

### ✅ СТАЛО (финал):
```python
if буфер_текущий < 1.1:
    order_month = i - cycle_m  # Когда заказать
    pending[i] = ckg  # Приход в текущем месяце
```
**Преимущества:** Просто, понятно, работает правильно!

## EDGE CASE: Начало горизонта

Если `i - cycle_m < 0` (например, апрель - 4 = -3):
```python
# Заказ был нужен ДО начала симуляции
# Размещаем экстренный приход в текущем месяце
pending[i] = ckg
order_month = 0  # Условно "в начале"
```

Это означает что **уже опоздали** с заказом — нужно было заказать раньше!

## В RESULTS СОХРАНЯЕТСЯ

```python
results[i] = {
    "containers": 1 или 0,
    "order_kg": 25000 или 0,
    "order_month": i - cycle_m,  # ← НОВОЕ ПОЛЕ
    ...
}
```

Теперь можем показать в Excel:
- **Приход** в месяце i (колонка "Приход")
- **Заказ размещён** в месяце order_month (колонка "Заказ → приход")

## ФАЙЛ

`simulation.py` — финальная логика проверки заказа
