# ИСПРАВЛЕНИЕ: Пересчёт остатков теперь работает правильно

## ПРОБЛЕМА

Петрушка (В), группа "Египет трава", июнь:
- Остаток на начало: 0 кг
- Приход: 5000 кг
- План: ~2000 кг

**Ожидалось:**
- Неделя 2 → остаток на июль: ~3200 кг
- Неделя 4 → остаток на июль: ~4200 кг

**Фактически:**
- Любая неделя → остаток на июль: 3050 кг (не меняется!)

## ПРИЧИНА

В коде проверялось условие **для ГРУППЫ**:
```python
if balance_start >= tpi:  # balance_start — остаток ВСЕЙ группы
    # Используем полный план для ВСЕХ позиций
```

**Египет трава** — это группа с 4 позициями:
- Петрушка (В): остаток 0 кг
- Укроп: остаток 2000 кг
- Кинза: остаток 1500 кг
- Базилик: остаток 500 кг

**Суммарный остаток группы:** 0 + 2000 + 1500 + 500 = 4000 кг  
**Суммарный план группы:** 3500 кг

**Условие:** `4000 >= 3500` → **TRUE**

Поэтому для **ВСЕХ позиций** (включая петрушку с остатком 0!) использовался **полный план**, а не пропорциональный!

## РЕШЕНИЕ

Изменил логику — теперь проверяем **КАЖДУЮ позицию отдельно**:

```python
# БЫЛО (НЕПРАВИЛЬНО):
if balance_start >= tpi:  # Проверка группы
    # Полный план для ВСЕХ позиций
    item_state[...] = item_balance + item_arrival - item_plan

# СТАЛО (ПРАВИЛЬНО):
if item_balance_start >= item_plan:  # Проверка ПОЗИЦИИ
    # Полный план только если У ЭТОЙ позиции хватало
    item_state[...] = item_balance + item_arrival - item_plan
elif item_arrival > 0:
    # Пропорциональный план если У ЭТОЙ позиции не хватало
    item_plan_after = calculate_proportional_plan(item_plan, arrival_day)
    item_state[...] = item_arrival - item_plan_after
```

## РЕЗУЛЬТАТ ТЕПЕРЬ

**Петрушка (В), июнь:**
- Остаток: 0 кг
- Приход: 5000 кг
- План: 2000 кг/мес

**Неделя 2 (10-е число):**
```
Условие: 0 >= 2000 → FALSE (товара не хватало)
Приход есть: 5000 > 0 → TRUE
День прихода: 10
План после: 2000 × (14/21) = 1333 кг
Остаток на июль: 5000 - 1333 = 3667 кг ✅
```

**Неделя 4 (20-е число):**
```
Условие: 0 >= 2000 → FALSE (товара не хватало)
Приход есть: 5000 > 0 → TRUE
День прихода: 20
План после: 2000 × (6/21) = 571 кг
Остаток на июль: 5000 - 571 = 4429 кг ✅
```

**Разница:** 4429 - 3667 = **762 кг** (теперь меняется!)

## ТЕСТИРОВАНИЕ

Проверь конкретно:
1. Египет трава, Петрушка (В), июнь
2. Измени неделю с 2 на 4
3. Остаток на начало июля должен **измениться**

## ФАЙЛ

`simulation.py` — исправленная версия с проверкой каждой позиции отдельно
