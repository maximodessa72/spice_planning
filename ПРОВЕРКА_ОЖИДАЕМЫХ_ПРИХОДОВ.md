# ИСПРАВЛЕНИЕ: Проверка ожидаемых приходов перед заказом

## ПРОБЛЕМА

**Китай овощи (цикл 4 месяца):**

| Месяц | Буфер | Заказ? | Приход | Результат |
|-------|-------|--------|--------|-----------|
| Май | 1.0 | ✅ Сен | — | Правильно |
| Июнь | 0.4 | ✅ Окт | — | Правильно |
| Июль | 0.1 | ✅ Ноя | — | Правильно |
| Август | 1.7 | ❌ | — | Правильно |
| Сентябрь | 2.2 | ❌ | 25000 | Много товара! |
| Октябрь | 3.2 | ❌ | 25000 | Много товара! |
| Ноябрь | 4.1 | ❌ | 25000 | Много товара! |
| Декабрь | 5.0 | ❌ | — | **ПЕРЕЗАПАС!** |

**Проблема:** В мае, июне, июле система заказала по контейнеру (буфер < 1.1), но все они пришли подряд (сен, окт, ноя) → перезапас (буфер 5.0)!

## ПРИЧИНА

Старая логика:
```python
if буфер < 1.1:
    заказываем  # Каждый месяц заказывали!
```

Не проверяла — **есть ли уже ожидаемый приход**!

**Сценарий:**
1. **Май:** буфер 1.0 < 1.1 → заказали (приход в сентябре)
2. **Июнь:** буфер 0.4 < 1.1 → заказали СНОВА (приход в октябре)
3. **Июль:** буфер 0.1 < 1.1 → заказали СНОВА (приход в ноябре)

Все 3 контейнера пришли подряд → перезапас!

## РЕШЕНИЕ

**Новая логика:**
```python
if буфер < 1.1:
    # Проверяем есть ли ожидаемые приходы в ближайшие cycle_m месяцев
    if НЕТ ожидаемых приходов:
        заказываем
```

### Код:

```python
if w_buf_before < order_threshold:
    # Проверяем есть ли ожидаемые приходы в ближайшие cycle_m месяцев
    has_pending_arrival = any(
        (i + k) in pending and pending[i + k] > 0
        for k in range(1, cycle_m + 1)
    )
    
    if not has_pending_arrival:
        # Заказываем только если нет ожидаемых приходов
        pending[i + cycle_m] = ckg
```

### Логика работы:

**Май (mi=1):**
```
Буфер: 1.0 < 1.1 → проверяем
Ожидаемые приходы в (май+1)..(май+4) = (июн..сен):
  pending[2] = нет
  pending[3] = нет
  pending[4] = нет
  pending[5] = нет
→ НЕТ ожидаемых → ✅ ЗАКАЗЫВАЕМ
→ pending[5] = 25000 (сентябрь)
```

**Июнь (mi=2):**
```
Буфер: 0.4 < 1.1 → проверяем
Ожидаемые приходы в (июн+1)..(июн+4) = (июл..окт):
  pending[3] = нет
  pending[4] = нет
  pending[5] = 25000 ← ЕСТЬ!
  pending[6] = нет
→ ЕСТЬ ожидаемый приход → ❌ НЕ ЗАКАЗЫВАЕМ!
```

**Июль (mi=3):**
```
Буфер: 0.1 < 1.1 → проверяем
Ожидаемые приходы в (июл+1)..(июл+4) = (авг..ноя):
  pending[4] = нет
  pending[5] = 25000 ← ЕСТЬ!
  pending[6] = нет
  pending[7] = нет
→ ЕСТЬ ожидаемый приход → ❌ НЕ ЗАКАЗЫВАЕМ!
```

## РЕЗУЛЬТАТ С НОВОЙ ЛОГИКОЙ

| Месяц | Буфер | Ожидаемый приход? | Заказ? | Результат |
|-------|-------|-------------------|--------|-----------|
| Май | 1.0 | Нет | ✅ Сен | Заказали |
| Июнь | 0.4 | **Да (Сен)** | ❌ | НЕ заказываем |
| Июль | 0.1 | **Да (Сен)** | ❌ | НЕ заказываем |
| Август | 1.7 | Да (Сен) | ❌ | НЕ заказываем |
| Сентябрь | X | — | Приход 25000 | Буфер восполнен |

**Только ОДИН контейнер** вместо трёх! ✅

## ПРЕИМУЩЕСТВА

1. **Нет перезапаса** — заказываем только когда действительно нужно
2. **Оптимальный буфер** — поддерживаем 1.0-1.5 мес вместо 5.0
3. **Экономия** — не замораживаем деньги в лишнем товаре

## ПАРАМЕТРЫ

- **Порог буфера:** 1.1 месяца
- **Проверка приходов:** на cycle_m месяцев вперёд
- **Минимум заказа:** 1 контейнер

## ФАЙЛ

`simulation.py` — проверка ожидаемых приходов перед заказом
