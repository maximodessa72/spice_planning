# ФИНАЛЬНАЯ ВЕРСИЯ simulation.py — Все исправления

## ВСЕ ИСПРАВЛЕНИЯ В ЭТОМ ФАЙЛЕ:

### 1. ✅ Пересчёт остатков с учётом недели прихода
**Строки ~40-95:** Добавлены функции:
- `get_arrival_day_from_week(wl)` → день прихода (5, 10, 15, 20)
- `calculate_proportional_plan(plan, day)` → пропорциональный план

**Строки ~474-491:** Пересчёт `item_state` с учётом недели:
```python
if item_balance_start >= item_plan:
    # Товара хватало — полный план
    item_state = item_balance + item_arrival - item_plan
elif item_arrival > 0:
    # Товара не хватало — пропорциональный план
    arrival_day = get_arrival_day_from_week(wl)
    item_plan_after = calculate_proportional_plan(item_plan, arrival_day)
    item_state = max(0, item_arrival - item_plan_after)
```

**Результат:** Остатки на следующий месяц зависят от недели прихода ✅

---

### 2. ✅ Балансировка с учётом недели прихода
**Строки ~258-330:** Функция `opt_std` с итеративным поиском T:

- Фиксированная неделя 2 (10-е число) для нефиксированных заказов
- Коэффициент 0.67 (14 дней из 21)
- Формула для позиций с остатком < план:
  ```python
  заказ = T × план + план × 0.67
  ```

**Результат:** Все позиции получают одинаковый буфер на конец месяца заказа И на начало следующего ✅

---

### 3. ✅ Упрощённая проверка необходимости заказа
**Строки ~493-506:** Новая логика:
```python
order_threshold = 1.1  # Порог буфера группы

if w_buf_before < order_threshold:
    # Заказываем контейнер
    pending[i + cycle_m] = ckg
```

**w_buf_before** — средневзвешенный буфер ГРУППЫ на начало месяца.

**Результат:** Если буфер группы < 1.1 мес → заказываем контейнер ✅

---

## ЛОГИКА РАБОТЫ СИСТЕМЫ

### Месяц с приходом контейнера:

1. **Балансировка** рассчитывает заказ с учётом недели 2:
   ```
   Заказ = T × план + план × 0.67 (если товара не хватало)
   ```

2. **Все позиции** получают буфер ≈ T на конец месяца

3. **Пересчёт остатков** тоже использует коэффициент 0.67:
   ```
   Остаток = приход - план × 0.67
   Буфер на след. месяц = остаток / план ≈ T
   ```

4. **Результат:** Буферы одинаковые на конец месяца заказа И на начало следующего! ✅

### Проверка необходимости заказа:

```
Каждый месяц:
  Буфер группы = средневзвешенный буфер всех позиций
  
  Если буфер < 1.1:
    → Заказываем контейнер (приход через cycle_m месяцев)
  Иначе:
    → НЕ заказываем
```

---

## ПАРАМЕТРЫ

| Параметр | Значение | Где |
|----------|----------|-----|
| Неделя прихода (по умолчанию) | Неделя 2 (10-е число) | opt_std, строка 268 |
| Коэффициент плана | 0.67 (14/21) | opt_std, строка 271 |
| Порог заказа | 1.1 месяца | run_simulation, строка 499 |

---

## ПРИМЕРЫ

### Пример 1: Балансировка с одинаковыми буферами

**Июль, Египет трава:**
```
Лемонграс: остаток 0, план 1800
Укроп (В): остаток 4213, план 5230
Мята: остаток 2404, план 2200

Балансировка с T=1.9, неделя 2 (коэфф. 0.67):
  Лемонграс: заказ = 1.9×1800 + 1800×0.67 = 4626 кг
  Укроп (В): заказ = 1.9×5230 + 5230×0.67 = 13441 кг
  Мята: заказ = 1.9×2200 + 2200×0.67 = 5654 кг

После прихода в июле:
  Все продадут: план × 0.67
  Остаток на август: приход - план×0.67
  Буфер на август: 1.9 для ВСЕХ ✅
```

### Пример 2: Проверка необходимости заказа

**Октябрь, Египет трава:**
```
Буфер группы на начало: 0.4 мес
Порог: 1.1 мес

0.4 < 1.1 → ✅ ЗАКАЗЫВАЕМ контейнер
Приход через 4 месяца (февраль)
```

---

## ФАЙЛ

`simulation.py` — все 3 исправления в одном файле:
1. Пересчёт остатков с учётом недели
2. Балансировка с учётом недели
3. Упрощённая проверка заказа

**ГОТОВО К ДЕПЛОЮ!** 🚀
